# Core dependencies
python-dotenv>=1.0.0
websockets>=12.0
aiohttp>=3.9.0

# OpenAI (LLM)
openai>=1.12.0

# Cloud providers (optional)
# Uncomment if using cloud providers
# deepgram-sdk>=3.0.0  # Alternative to raw websockets for Deepgram

# Local STT (Faster Whisper)
faster-whisper>=1.0.0
numpy>=1.24.0

# Local TTS (Piper)
piper-tts>=1.2.0
